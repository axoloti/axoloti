Scripts in this directory
-------------------------

build.sh 
Downloads and builds all needed mac-specific tools. Requires a minimal xcode install.

distclean.sh
removes all unneeded source files, but keeps the executables

mrproper.sh 
removes all the source files and the executables
